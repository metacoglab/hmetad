# Get the likelihood parameterization in `model`

Get the likelihood parameterization in `model`

## Usage

``` r
get_ll(model)
```

## Arguments

- model:

  The `brms` model to get the parameterization for

## Value

A character vector, either `"multinomial"` or `"categorical"`.
