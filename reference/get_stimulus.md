# Get the stimulus variable in `model`

Get the stimulus variable in `model`

## Usage

``` r
get_stimulus(model)
```

## Arguments

- model:

  The `brms` model to get the variable for

## Value

A character vector.
